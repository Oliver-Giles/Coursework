object UseBirthday {
  def main(args: Array[String]): Unit = {
    //var person1: Birthday = new Birthday()
   // var person2: Birthday = new Birthday()

    //person1.setDetails("Oliver Giles", 7, 5, 2004)
    //person2.setDetails("Jack Smith", 5, 12, 2003)
    //println(f"person1's details are: ${person1.getDetails.productIterator.mkString(", ")}")
    //println(f"\nperson2's name is: ${person2.getName}")
    //println(f"person2's birthday is: ${person2.getBirthday.productIterator.mkString(", ")}")

  }
}
